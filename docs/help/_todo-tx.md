# Transmitting *(not yet implemented)*

This section is a placeholder. When the TX path ships, document:

- PTT methods (button / VOX / CAT / CW key / foot switch)
- SSB modulation chain (mic → compressor → filter → modulator → DUC)
- CW keyer settings (iambic A/B, speed, weight, side-tone)
- RTTY / FSK / DIG (BPSK, QPSK, other)
- Mic path selection (AK4951 mic on HL2+ vs PC sound-card mic)
- Mic gain, DX / compression / processor settings
- Monitor level
- Tune power (CW carrier out) for alignment
- PA protection (fault monitoring, thermal, SWR cutoff)
- Pre-distortion (PureSignal equivalent) — if it lands

---

*File name starts with `_todo-` so we know at a glance this is a stub.*
