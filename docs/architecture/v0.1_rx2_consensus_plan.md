# v0.1 RX2 Consensus Plan — Three-Engineer Review

**Status:** AUTHORITATIVE. This document supersedes `v0.0.9_rx2_plan.md`
and `rx2_research_notes.md` for v0.1 (RX2), v0.2 (TX), and v0.3
(PureSignal) implementation work.

**Date locked:** 2026-05-05.

**Reviewers:** Three independent senior-engineering passes — protocol
layer, DSP architecture, UI/operator-model — followed by a second
accountability pass with cross-layer verification. Both rounds
documented in chat-record from 2026-05-05.

**Methodology:** Round 1 mapped each layer of Thetis's HL2 implementation
independently. Round 2 cross-checked round 1's findings, including
challenging each other's claims. Six errors were caught and corrected
between rounds (see §10). Three first-try-failure-mode bench tests
were identified as **mandatory** before v0.3 PureSignal can be
declared ready (see §9.3).

---

## Table of contents

1. [Decisions reference (settled)](#1-decisions-reference)
2. [Channel ID convention (locked)](#2-channel-id-convention)
3. [Phase 0 — Foundation (v0.1.0-pre1)](#3-phase-0-foundation)
4. [Phase 1 — RX2 protocol enablement (v0.1.0-pre2)](#4-phase-1-rx2-protocol-enablement)
5. [Phase 2 — RX2 audio routing (v0.1.0-pre3)](#5-phase-2-rx2-audio-routing)
6. [Phase 3 — RX2 UI integration (v0.1.0-pre4)](#6-phase-3-rx2-ui-integration) — incl.
   Tuning panel evolution v0.0.9.3 → v0.3 (§6.5)
7. [Phase 4 — RX2 polish (v0.1.0)](#7-phase-4-rx2-polish) — incl.
   meter system cleanup (§7.1)
8. [v0.2 TX path — touchpoints from RX2](#8-v02-tx-path) — incl.
   TX multi-meter activation (§8.4)
9. [v0.3 PureSignal — bench gates and architecture](#9-v03-puresignal)
10. [Errors round 1 had — corrections from round 2](#10-errors-corrected)
11. [Patterns Lyra adopts vs diverges from Thetis](#11-patterns-vs-divergences)
12. [Cross-references](#12-cross-references)

---

## 1. Decisions reference

These are settled by consensus. No further debate without explicit
re-evaluation captured here.

### 1.1 Channel ID convention (host-side, NOT wire-protocol DDC index)

Lyra reserves the following **host channel IDs** from v0.1 phase 0
forward. See §2.1 for the critical distinction between host channel
IDs and wire-protocol DDC indices — they are NOT the same numbering.

| Host ID | Role | Used in |
|---|---|---|
| 0 | RX1 main | v0.0.9.x (existing) |
| 1 | RX1 sub-receiver | reserved (post-v0.3) |
| 2 | RX2 main | v0.1 |
| 3 | (HL2) PS feedback target during PS-active OR (future ANAN) RX2-sub | reserved |
| 4 | TX main (TXA) | v0.2 |
| 5 | PS feedback A on future ANAN 5-DDC | reserved |
| 6 | PS feedback B on future ANAN 5-DDC | reserved |

**On HL2 (4-DDC, the only Lyra v0.1-v0.3 target):** PS feedback
flows through `twist()`-interleaved DDC2+DDC3 → host channel 3.
RX2-sub is not enabled on HL2 (Thetis convention; verified round 2).
Slots 5 and 6 stay reserved for v0.4 ANAN support.

### 1.2 Architectural building blocks

These three pieces of infrastructure must land in their respective
phases or downstream phases will require refactor:

- **AAmixer-equivalent stream router** (§3.3 in this doc): a
  2-D state matrix `(stream × destination)` gated by
  `(power, MOX, diversity, PS_enabled, RX2_enabled)`. Lyra
  currently has a single one-RX-to-stereo path. New module.
  **Indexing scheme decision (locked):** stream IDs are the
  Lyra host channel IDs from §2.2 (0 = RX1, 2 = RX2, 4 = TX-mon,
  3/5/6 for PS feedback). Lyra does NOT introduce a separate
  `mixinid()`-equivalent index space (the way Thetis does in
  cmaster.c:533, 552). Rationale: Thetis's two-axis indexing is
  a WinForms-era artifact; Lyra's host channel IDs are stable
  and unique enough to serve as both DSP channel keys AND mixer
  stream keys without collision.
- **Per-channel patchpanel block** (§5.1): the audio pan / binaural
  / I-Q-copy block at the END of each RX's DSP chain. Pan curve
  per WDSP `SetRXAPanelPan`. **NOT at the audio sink** — at the
  end of each channel's mono chain.
- **TX I/Q tap upstream of (future) iqc** (§8.2): a tap point that
  copies pre-predistorter TX I/Q into a snapshot ring. Must land
  in v0.2 even though iqc/calcc don't exist until v0.3. Otherwise
  v0.3 forces a TX-path refactor.

### 1.3 Threading model

- **v0.1, v0.2:** keep Lyra's existing single DSP worker thread
  with per-channel state. Multiplex per-channel work in the same
  worker.
- **v0.3:** switch to **per-channel worker** following Thetis's
  `_beginthread(wdspmain, ...)` pattern. PS feedback DDCs need
  parallel time-aligned snapshot capture against TX I/Q; muxing
  in a single worker introduces variable-latency between the two
  snapshot streams that calcc cannot tolerate.

### 1.4 UI deliberate divergence from Thetis

Lyra's v0.1 UI is NOT a Thetis port — these are documented
deliberate divergences:

| Lyra | Thetis | Justification |
|---|---|---|
| Hybrid focus model: single Mode+Filter / DSP+Audio panel + per-RX status badges + click-to-focus + Ctrl+1/Ctrl+2 hotkeys | Duplicated controls per RX (`comboAGC` + `comboRX2AGC`, etc.) | Smaller layout, halves disabled-control surface, cleaner in PySide6 layouts |
| Per-RX display pan + per-RX zoom | Global display pan, per-band-per-RX zoom | Asymmetry in Thetis is a WinForms artifact; PySide6 makes per-RX trivial |
| Visible AAmixer-state indicator | None — operator infers from chkPower/chkMOX/chkRX2/PS button states | UX win; Lyra adds small status badge |
| PS hardware-mod attestation checkbox | None | Safety improvement Thetis does not have |
| QSplitter for panadapter regions | Skin-relative percentage layout (`pan_rx2_basis + h_delta * 0.383`) | Qt-native idiom |

### 1.5 UI patterns Lyra DOES adopt verbatim

| Pattern | Source |
|---|---|
| `TXFreq` derivation as pure function of `(rx2_enabled, VFOATX, VFOBTX, VFOSplit, VFOA, VFOB, VFOASub)` | console.cs:11396-11420 |
| Independent per-pan click-tune flags | console.cs:51469-51470 |
| `MuteRX1OnVFOBTX` / `MuteRX2OnVFOATX` as independent operator toggles | console.cs:40494, 40506 |
| TUN button label flip ("TUN" ↔ "AUTO") for HL2 auto-tune | console.cs:25702/25773 |
| Force-toggle SPLIT off when entering FM | various |
| Force `VFOATX = true` when `SPLIT + RX2 + VFOBTX` requested | console.cs:36534-36541 |
| 8-state PS command FSM + 3-state AA FSM | PSForm.cs:111-127 |
| Per-band/per-rig PS correction snapshot Save/Restore | PSForm.cs |

---

## 2. Channel ID convention

### 2.1 Two distinct numbering axes — DO NOT CONFLATE

Lyra has TWO numbering systems that look similar but mean different
things. Confusing them is a near-certain implementation bug:

- **Wire-protocol DDC index** (0, 1, 2, 3) — refers to the four
  DDCs in the HL2's gateware, addressed via C&C frames 2 (DDC0),
  3 (DDC1), 5 (DDC2), 6 (DDC3). This is what CLAUDE.md §6.4
  refers to with `ddc[0].freq_source = VFOA`. EP6 sample slots
  carry DDC0..DDC3 I/Q in a fixed wire-format order.
- **Host-side channel ID** (0..6) — refers to Lyra's per-channel
  DSP state objects (instances of `lyra/dsp/channel.py`). Used
  internally to route audio, configure DSP per-receiver, and key
  the AAmixer routing matrix.

**The mapping between them is NOT identity:**

| Wire DDC | Host channel ID | Role |
|---|---|---|
| DDC0 | 0 | RX1 |
| DDC1 | 2 | RX2 (skips host channel 1 which is reserved for RX1-sub) |
| DDC2 | 5 (or 3 alias on 4-DDC HL2) | PS feedback A |
| DDC3 | 6 (or 3 alias on 4-DDC HL2) | PS feedback B |

The EP6 dispatch in §4.2 uses a `twist()` helper to interleave
DDC2+DDC3 pairs into a single feedback stream — that stream is
delivered to ONE host channel, not two, on HL2. Plan §9.1
clarifies which slot holds the feedback stream during PS.

### 2.2 Locked host channel ID assignments

```
Lyra host channel ID assignments (LOCKED at v0.1 phase 0):

  0 = RX1 main                                    [active in v0.0.9.x]
  1 = RX1 sub-receiver                            [reserved, post-v0.3]
  2 = RX2 main                                    [active in v0.1]
  3 = (4-DDC HL2 only) PS feedback alias OR
      (5-DDC ANAN future) RX2 sub-receiver        [reserved]
  4 = TX main (TXA)                               [active in v0.2]
  5 = PS feedback A on 5-DDC ANAN (future)        [reserved]
  6 = PS feedback B on 5-DDC ANAN (future)        [reserved]
```

**On HL2 (4-DDC, the only Lyra-targeted hardware in v0.1-v0.3):**
PS feedback flows through the `twist()`-interleaved DDC2+DDC3
stream and is delivered to **host channel 3** during PS-active.
RX2-sub is not enabled on HL2 (Thetis convention; round-2 verified
at `GetDDC` in console.cs:8579-8775).

**On future ANAN 5-DDC hardware (v0.4+):** PS feedback A goes to
host channel 5, PS feedback B to host channel 6, leaving slot 3
free for RX2-sub. The slot map is already correct for that case.

**Round 1 error caught in round 2:** TX host channel ID was
reported as `10` (CMsubrcvr × CMrcvr = 2 × 5). Round 2 verified
Thetis source shows `2 × 2 = 4` for the standard config (CMrcvr=2,
CMsubrcvr=2 on HL2). Plan now correct.

---

## 3. Phase 0 — Foundation (v0.1.0-pre1)

**Goal:** Multi-channel scaffolding with NO behavior change vs
v0.0.9.3. Polish items from v0.0.9.4 backlog ride along.

### 3.1 What lands in this pre-release

**Polish items (folded in per Rick's decision 2026-05-05):**

- Startup modal dialog for unseen-version updates (replaces the easy-
  to-miss status-bar toast). Skipped-versions persist in QSettings.
- Console log when silent update check finds a new release.
- Settings dialog QCheckBox/QLabel lambda crash fixes (the
  `apf_enable_chk` and `action_label` "object already deleted"
  lambdas Brent and Rick both hit on v0.0.9.x).
- Animated toolbar update indicator (5-second pulse on first
  appearance).
- General sweep of remaining debug prints from v0.0.9.x sessions.

**Multi-channel refactor (no behavior change):**

- Reserve channel IDs per §2.
- `lyra/dsp/channel.py`: refactor to be instantiable per channel ID.
  Today's single-channel code becomes "channel 0" with the same
  behavior; channel 2 stub exists but isn't wired to anything.
- AAmixer **stub** in `lyra/dsp/mixer.py` (new file): exposes
  `set_route(stream_id, dest_mask)` and `set_state(power, mox,
  diversity, ps_enabled, rx2_enabled)`. Initially has only the
  RX1→sink route active; everything else returns no-op. Wired into
  the audio chain so future phases just add states.
- Per-channel patchpanel **stub** in `lyra/dsp/patchpanel.py` (new
  file): mono-input → (I, Q) lane pair. Initially in passthrough
  mode (I = audio, Q = 0). RX1 chain calls into it before audio
  reaches the AAmixer/sink.
- `lyra/protocol/stream.py`: keep `nddc=4` (already in place from
  v0.0.9.x), add a stub `_set_rx2_freq(hz)` that updates
  `_cc_registers[0x03]` (DDC1 freq) but otherwise does nothing
  observable until phase 1.

### 3.2 What does NOT change in phase 0

- Audio output is still single-RX (RX1).
- UI shows no RX2 controls.
- VFO B remains "shadow freq" mode (no real RX2 yet).
- Operators upgrading should hear/see no audible difference.

### 3.3 Risks for phase 0

- **AAmixer stream-ID indexing**: Thetis uses `mixinid()` (cmaster.c:533,
  552), a separate index space from WDSP's `id()`. Lyra must pick
  one indexing scheme and stick with it. Round-2 DSP agent flagged
  this as the #1 phase-0 first-try-failure mode.
- **Patchpanel placement**: putting pan at the audio sink instead
  of in-channel will fight the AAmixer in phase 2. Round-2 DSP
  agent confirmed Thetis places this in-channel (`patchpanel.c:158-
  176`, `RXA.c:531-542`). **Lyra MUST follow this placement.**
- **Lambda lifetimes in Settings dialog**: the QCheckBox/QLabel
  crashes are the same pattern. Single coordinated fix needed:
  disconnect signals on dialog close, or use `weakref` lambdas.

---

## 4. Phase 1 — RX2 protocol enablement (v0.1.0-pre2)

**Goal:** Activate RX2 at the wire-protocol level. Audio still goes
to RX1 only.

### 4.1 Protocol changes

- Confirm `nddc = 4` continues to be the default; no change to the
  C&C round-robin C4 byte (already 0x1C: bits[5:3] = `(nddc-1)<<3`
  = 0x18 + duplex bit 0x04 = 0x1C). Verified at
  `networkproto1.c:639` (Hermes) and `:967-968` (HL2 path).
- Wire `_set_rx2_freq(hz)` to write the operator's VFO B frequency
  into `_cc_registers[0x03]` (DDC1 freq, frame 3, addr 0x03).
- Confirm shared sample rate: HL2 has only one rate selector. RX1
  and RX2 cannot run independent rates. Verified at
  `netInterface.c:1328` and `networkproto1.c:949`. Lyra UI must
  reflect this (single Rate combo, not per-RX).
- Confirm priming: `ForceCandCFrame(3)` actually emits **6 frames
  (3 TX-VFO + 3 RX-VFO)** in 2 batches with 10 ms gaps
  (`networkproto1.c:134-139, 430`). Round-1 docs said "3 frames" —
  corrected.

### 4.2 EP6 dispatch

The 19-slot × 26-byte EP6 layout at nddc=4 must dispatch four DDC
streams. Per `networkproto1.c:383-387` (HL2 read) and `:549-553`,
the dispatch is:

```
DDC0 → audio chain ID 0  (RX1)
DDC2 + DDC3 (twisted)  → audio chain ID 1  (PS feedback)
DDC1 → audio chain ID 2  (RX2)
```

Note the **non-sequential router IDs**. Lyra's `lyra/protocol/stream.py`
currently dispatches DDC0 only; phase 1 adds:

- `twist(samples, ddc_a, ddc_b, dest_id)` helper matching
  `networkproto1.c:263-274` semantics — interleaves two DDCs into
  a 4-channel buffer (I0,Q0,I1,Q1) for the destination.
- Three dispatch calls per parsed EP6 frame: DDC0→0, twist(2,3,1),
  DDC1→2.

In phase 1, dispatch ID 1 (PS feedback) is simply discarded
(phase 3 wires it). Dispatch ID 2 (RX2) goes to channel 2's input.
Channel 2 has a stub DSP chain that produces zero audio (audio
routing comes in phase 2).

### 4.3 Risks for phase 1

- **Byte stride math**: at nddc=4, EP6 sample-set stride is
  `6 × nddc + 2 = 26 bytes` per slot, 19 slots per USB block. Mic
  is at offset `8 + nddc × 6` per slot. Off-by-one here is the
  most common first-try failure mode. Round-2 protocol agent
  identified this explicitly.
- **PTT-edge handling**: at nddc=4, the `out_control_idx = 2` jump
  that nddc=2 mode uses (`networkproto1.c:886-891`) is **skipped**.
  This is correct because DDC0 stays on RX1 freq at nddc=4 — but
  Lyra's existing code may have inherited the nddc=2 jump logic;
  needs audit.
- **`I2C inline branch`**: C0 bit 7 of returned C&C bytes signals
  I2C readback data, NOT regular telemetry. Parser must check
  bit 7 BEFORE dispatching on `C0 & 0xf8` for telemetry sub-cases
  (`networkproto1.c:478-493`). Easy to miss; corrupts FwdPower /
  RevPower / supply / temp readouts if missed.
- **Mic decimation counter persistence**: HL2 mic is in EP6 at
  a different rate than I/Q; decimation counter must persist across
  EP6 frames (`networkproto1.c:278, 400-410, 566-576`).
- **Watchdog**: 3-second EP6 silence triggers `destroy_pro` (line
  443). RX2 work doesn't change this risk; band changes during
  phase 1 testing should not stall EP6 reads >3s.

### 4.4 Phase 1 verification protocol

Phase 1 success criteria (each must pass before declaring phase
complete):

1. **DDC0 → host channel 0 dispatch.** Tune RX1 to a known carrier
   (e.g., WWV at 10.000 MHz). Verify channel 0's input stream
   shows a tone at the expected baseband offset. **Pass:** carrier
   visible at correct frequency in channel 0's spectrum sample.
2. **DDC1 → host channel 2 dispatch.** Tune VFO B to a different
   known carrier (e.g., WWV at 15.000 MHz). Verify channel 2's
   input stream shows that carrier at the expected baseband offset.
   **Pass:** different carrier from #1, at the right frequency, in
   the right channel.
3. **DDC2+DDC3 (twist) → host channel 3 dispatch (no PS).** Without
   PS armed, verify channel 3's input is reachable but produces no
   audio (the dispatch path discards the samples or routes them to
   a noise sink). **Pass:** no crash, no audio glitches, channel 3
   stream has data but is silently consumed.
4. **EP6 byte stride.** With both RX1 and RX2 active on different
   carriers, capture 1 second of EP6 frames and verify slot offsets
   produce the expected I/Q sample values per slot. **Pass:** no
   off-by-one in slot or DDC byte indexing — RX1 spectrum looks
   like RX1's tuned carrier (not RX2's, which would indicate a
   stride bug putting DDC1 samples where DDC0 samples belong).
5. **PTT-edge regression.** Toggle MOX briefly with both RX1+RX2
   active; verify both RX1 and RX2 audio remain on their correct
   carriers when MOX releases. **Pass:** no DDC freq glitch on PTT
   transition; freq returns to operator-set values within ~140 ms.

### 4.5 Confidence (round 2)

**Protocol agent: 90%** for phase 1 RX2. The protocol moves are
mechanical; main first-try-failure risk is byte stride math
(verification step 4 catches this).

---

## 5. Phase 2 — RX2 audio routing (v0.1.0-pre3)

**Goal:** RX2 produces audio. Default routing: RX1 hard-left,
RX2 hard-right.

### 5.1 Per-channel patchpanel block

Each RX channel gets its own patchpanel block at the end of its
DSP chain (after all per-RX DSP: notches, demod, NR, AGC, APF, etc.).
The block:

- Takes mono float32 audio (channel's output).
- Produces (I, Q) lane pair per the WDSP pan curve from
  `patchpanel.c:55-101, 158-176`:
  - `pan ≤ 0.5`: `gain1 = 1, gain2 = sin(pan × π)`
  - `pan > 0.5`: `gain1 = sin(pan × π), gain2 = 1`
  - `output_I = audio × gain1, output_Q = audio × gain2`
- Has independent operator-controllable `pan` per channel.
- Default for v0.1: RX1 `pan = 0` (hard-left, all in I lane);
  RX2 `pan = 1` (hard-right, all in Q lane).

The patchpanel block also handles binaural and I-Q-copy modes
(future, v0.1.0+ polish).

### 5.2 AAmixer stream router

Activates more routes in phase 2:

| Stream | Default destination |
|---|---|
| Channel 0 (RX1) `pan` output | I lane of stereo audio sink |
| Channel 2 (RX2) `pan` output | Q lane of stereo audio sink |

The mixer state matrix is gated by:

- `power` (off → all streams muted)
- `MOX` (TX active → optionally mute one or both RX per
  `MuteRX1OnVFOBTX` / `MuteRX2OnVFOATX` operator toggles)
- `diversity` (when on, RX2 routing is reconfigured but the
  operator's RX2-enable state is preserved per round-2 UI agent
  finding)
- `PS_enabled` (during PS+MOX, both RX audio routings are disabled
  per console.cs:28307)
- `RX2_enabled` (off → channel 2 stream output muted but channel
  still runs)

### 5.3 Audio chain integration

```
[Channel 0]  decim → notches → demod → LMS → ANF → SQ → NR
                  → AF → AGC → Vol → APF (CW) → leveler
                  → patchpanel → (I, Q) → AAmixer route 0 → L

[Channel 2]  decim → notches → demod → LMS → ANF → SQ → NR
                  → AF → AGC → Vol → APF (CW) → leveler
                  → patchpanel → (I, Q) → AAmixer route 2 → R
                                                               → tanh → sink
```

Note: each channel has its OWN AGC and APF instance. Each channel
independently configurable (different mode, different filter,
different AGC profile per the focused-RX UI model in phase 3).

### 5.4 Risks for phase 2

- **Pan placement**: must be per-channel, before AAmixer. Round-2
  DSP agent confirmed this is the only placement that allows
  binaural mode to work later. Putting pan at the sink stage
  breaks binaural and double-pans against the AAmixer.
- **Sample-rate consistency**: both channels operate at 48 kHz
  audio output (HL2 codec's fixed rate). The decimator inside
  each channel handles input rate variance.
- **Identical DSP chain duplication**: phase 2 essentially clones
  channel 0's chain into channel 2. Audit that all per-channel
  state is in the channel object (no module-globals); recent
  WDSP AGC swap left some legacy `_noise_baseline` field on Radio
  that's not actively used but should be moved to per-channel
  state in this phase.

### 5.5 Confidence (round 2)

**DSP agent: HIGH.** Phase 2 is the cleanest of the v0.1 phases.
Main risk is the patchpanel placement; Round 2 has that nailed
down.

---

## 6. Phase 3 — RX2 UI integration (v0.1.0-pre4)

**Goal:** Operator-facing controls for RX2.

### 6.1 Hybrid focus model

(Re-stated from §1.4.) Single Mode+Filter and DSP+Audio panel
operate on the focused RX. Click any RX freq display to focus.
Hotkeys Ctrl+1 → focus RX1, Ctrl+2 → focus RX2. Per-RX status
badges (mode, filter abbreviation, AGC profile) display read-only
on each freq display.

This is a **deliberate divergence** from Thetis's duplicated-
controls model (verified by round 2 UI agent finding 35
`comboRX2*`/`chkRX2*` instances).

### 6.2 A/B/Swap/SPLIT state machine

Lyra implements `tx_freq()` as a pure function of state per the
TXFreq derivation table at `console.cs:11396-11420`:

```python
def tx_freq(rx2_enabled: bool, vfoatx: bool, vfobtx: bool,
            split: bool, vfoa: int, vfob: int, vfoasub: int) -> int:
    if not rx2_enabled:
        if vfobtx:
            return vfob
        return vfoa
    # rx2_enabled
    if vfobtx:
        return vfob
    if vfoatx and split:
        return vfoasub
    return vfoa
```

Invariants:

- VFOATX and VFOBTX mutually exclusive (radio-style).
- SPLIT + RX2 + VFOBTX requested → force VFOATX = true
  (per console.cs:36540).
- SPLIT off without RX2 → VFOSync forced off.
- During MOX, VFO setters early-return when `MOX && (VFOATX ||
  (!RX2_enabled && VFOBTX))`.

Buttons: A→B, B→A, Swap (full state when RX2 enabled, freq+mode+
filter only when RX2 disabled — VFO B is virtual in that case).

### 6.3 Panadapter splitting

Vertical QSplitter, RX1 on top, RX2 on bottom. Both span full
window width. Splitter handle is operator-draggable. (Thetis uses
fixed skin-relative percentages; Lyra uses Qt-native QSplitter as
a deliberate divergence per §1.4.)

Each RX panadapter is an independent instance:

- Per-RX zoom (per-band-per-RX persistence)
- Per-RX display pan (deliberate divergence from Thetis's global
  display pan per §1.4)
- Per-RX dB range (top/bottom sliders)
- Per-RX click-to-tune dispatched by hit-test rectangle
  (independent flags `_click_tune_display` / `_click_tune_rx2_display`)

### 6.4 Click-tune divergence

Per round-2 UI agent: Thetis's single `PanDisplay` class with
`static current_click_tune_mode` and `static click_tune_filter`
makes click-tune behavior identical between pans by construction.
Lyra MUST mirror this — if click-tune logic diverges between RX1
and RX2 panadapters, operator gets confusing inconsistency.

**Reconciling §1.5 ("independent per-pan click-tune flags") with
this section:** the two are not in conflict. The independent
flags refer to **hit-test routing** (which pan was clicked → which
RX gets tuned). The shared logic refers to **click-tune behavior**
(snap mode, snap range, modifier-key model, filter selection
during click).

Implementation: ONE shared "click-tune mode" state object
(snap radius, peak-detect threshold, modifier mapping); TWO
separate hit-test rectangles + per-pan click handlers that each
consume the shared mode and dispatch the tune action to the
correct RX channel. This matches Thetis's `PanDisplay` class
reuse (single class, two instances, shared static state).

### 6.5 Tuning panel evolution

The Tuning panel (`lyra/ui/panels.py:TuningPanel`) is the central
VFO display + tuning control surface.  It evolves through the
v0.1 / v0.2 / v0.3 release cycle.  The current scaffolding
already has placeholder slots for RX2 and TX (RX2 freq display
disabled with "RX2 — not yet wired" badge; TX SPLIT row hidden).
This subsection documents the operator-perceptible evolution so
implementation work hits a known target.

**v0.0.9.3 (today — RX1 only):**

```
┌────────────────┬──────────┬────────────────┐
│ RX1            │  [logo]  │ RX2            │
│ 7.074.000      │          │ — not yet wired│
│                │          │  (disabled)    │
├────────────────┴──────────┴────────────────┤
│ TX1 SPLIT — off —                          │ ← hidden
├─────────────────────────────────────────────┤
│ MHz [7.074000]  Step [1 kHz ▼]             │
└─────────────────────────────────────────────┘
```

**v0.1.0 (RX2 active, TX not yet):**

```
┌────────────────────┬──────────┬────────────────────┐
│ RX1   [USB] 2.4k   │  [logo]  │   RX2   [USB] 2.4k │
│ ╔═══════════════╗  │          │  ╔═══════════════╗ │ ← cyan border
│ ║ 7.074.000     ║  │          │  ║ 14.074.000    ║ │   = focused RX
│ ╚═══════════════╝  │          │  ╚═══════════════╝ │
│ AGC: Med           │          │ AGC: Slow          │ ← read-only badges
├────────────────────┴──────────┴────────────────────┤
│  [A→B]  [B→A]  [Swap]    [🔒 Lock B]               │ ← VFO ops row
├─────────────────────────────────────────────────────┤
│ MHz [7.074000]  Step [1 kHz ▼]                      │ ← operates on FOCUSED RX
└─────────────────────────────────────────────────────┘
```

Behaviors landing in v0.1.0:

- Click either freq display → focus that RX.  Cyan border indicates
  focus.  Hotkeys: Ctrl+1 → RX1, Ctrl+2 → RX2.
- Status badges below each freq display (read-only): mode, filter
  BW, AGC profile.  Glanceable per-RX state.
- **Single MHz type-in + Step combo operates on the focused RX**
  (deliberate divergence from Thetis per §1.4 — Thetis duplicates
  the controls per RX).
- A→B / B→A / Swap / Lock B row appears when RX2 is enabled,
  hidden when RX2 is off.
- Focus also follows panadapter clicks (clicking RX2 panadapter
  region focuses RX2).

**v0.2.0 (RX2 + TX SSB):**

```
┌────────────────────┬──────────┬────────────────────┐
│ RX1   [USB] 2.4k   │  [logo]  │   RX2   [USB] 2.4k │
│ ╔═══════════════╗  │          │  ╔═══════════════╗ │
│ ║ 7.074.000  •TX║  │          │  ║ 14.074.000    ║ │ ← amber dot = TX VFO
│ ╚═══════════════╝  │          │  ╚═══════════════╝ │
│ AGC: Med           │          │ AGC: Slow          │
├────────────────────┴──────────┴────────────────────┤
│  [A→B]  [B→A]  [Swap]    [🔒 Lock B]               │
├─────────────────────────────────────────────────────┤
│ ▸ TX1   7.074.000 USB    [● VFOATX]  [○ VFOBTX]    │ ← TX freq strip
│   SPLIT [○]  XIT [+0]    RIT [+0]                  │
├─────────────────────────────────────────────────────┤
│ MHz [7.074000]  Step [1 kHz ▼]                      │
└─────────────────────────────────────────────────────┘
```

What's new in v0.2.0:

- **Amber dot (•TX)** on whichever VFO is the current TX target,
  derived from the `tx_freq()` pure function (§6.2).  Moves
  automatically when SPLIT or VFOATX/VFOBTX toggle.
- **TX freq strip** (was hidden placeholder) becomes visible.
  Shows effective TX frequency, mode, the VFOATX/VFOBTX radio,
  SPLIT toggle, XIT, RIT.  TX freq display is read-only — the
  operator changes it indirectly via VFO A / VFO B / sub-VFO
  controls per the truth table.
- **MOX and TUN buttons go on the TOOLBAR**, NOT in the Tuning
  panel (deliberate divergence from Thetis per §1.4).  Toolbar
  placement keeps them accessible regardless of which panel
  has focus.

**v0.2.0 during active MOX:**

```
┌────────────────────┬──────────┬────────────────────┐
│ RX1   [USB] 2.4k   │  [logo]  │   RX2 [muted]   ⚠ │
│ ╔═══════════════╗  │          │  ╔═══════════════╗ │ ← red shadow on muted
│ ║ 7.074.000  •TX║  │ 🔴 ON AIR│  ║ 14.074.000    ║ │   (operator's
│ ╚═══════════════╝  │          │  ╚═══════════════╝ │    MuteRX2OnVFOATX
│ AGC: Med           │          │ AGC: Slow          │    is on)
├────────────────────┴──────────┴────────────────────┤
│  [A→B]  [B→A]  [Swap]    [🔒 Lock B]               │ ← buttons disabled
├─────────────────────────────────────────────────────┤
│ ▸ TX1   7.074.000 USB    [● VFOATX]  [○ VFOBTX]    │
│   SPLIT [○]  XIT [+0]    RIT [+0]                  │
├─────────────────────────────────────────────────────┤
│ MHz (locked)  Step [1 kHz ▼]                        │ ← MHz spinbox disabled
└─────────────────────────────────────────────────────┘
```

During MOX:

- **🔴 ON AIR indicator** in the logo position (pulsing red
  overlay over the Lyra logo).
- **Muted RX visually faded** with `[muted]` badge if the
  operator's `MuteRX1OnVFOBTX` / `MuteRX2OnVFOATX` toggle caused
  it (§5.2).  RX channel still runs internally; only audio
  routing is muted.
- **VFO ops + MHz spinbox locked.** Per the `console.cs:6485`
  invariant — VFO setters early-return during MOX.
- **TX freq readout stays unchanged** during keying — operator's
  commit is live; no surprise re-tuning mid-transmit.

**v0.3 (PureSignal active during MOX):**

```
┌────────────────────┬──────────┬────────────────────┐
│ RX1   [USB] 2.4k   │  [logo]  │   RX2  [muted]  ⚠ │
│ ╔═══════════════╗  │ 🔴 ON AIR│  ╔═══════════════╗ │
│ ║ 7.074.000  •TX║  │  PS 🟢   │  ║ 14.074.000    ║ │ ← green PS dot
│ ╚═══════════════╝  │          │  ╚═══════════════╝ │   = corrections live
│ AGC: Med           │          │ AGC: Slow          │
├────────────────────┴──────────┴────────────────────┤
│ [...same as v0.2 MOX layout above...]              │
└─────────────────────────────────────────────────────┘
```

PS visual states (small dot under ON AIR):

- 🟢 **Lime** — PS feedback received, calcc converged,
  corrections actively applied.  Healthy state.
- 🟡 **Yellow** — PS feedback received but calcc not yet
  converged, OR corrections applied but stale.  Watch state.
- 🔴 **Red** — PS feedback NOT seen.  Likely cause: hardware mod
  missing, cabling fault, or `cntrl1` routing failure (§9.3 Gate
  1).  Triggers a one-time toast: "PureSignal: no feedback
  detected — verify hardware mod."

Same information density as Thetis's `lblPSInfoCO` color-coded
indicator (PSForm.cs:579-592), surfaced in the always-visible
Tuning panel rather than buried in a PS dialog.

**Implementation notes for Phase 3 (UI integration):**

- The freq-display widget (`FrequencyDisplay`) already supports
  `set_vfo_enabled(bool, label)` for the disabled-placeholder
  state.  Live activation of RX2 is a `set_vfo_enabled(True)`
  call plus wiring the radio's RX2 freq signal.
- The TX SPLIT row (`tx_split_row`) is built and hidden today
  (`panels.py:371-386`).  Phase 3 makes it visible and adds the
  VFOATX/VFOBTX radio + SPLIT/XIT/RIT controls.
- Focus border styling (cyan) is a CSS update on `FrequencyDisplay`
  with a `setProperty("focused", True/False)` toggle.
- ON AIR indicator is a new overlay widget on the logo container
  (`logo_container` in `panels.py:319`); positioned absolutely,
  shown/hidden via `setVisible()` on MOX state change.

### 6.6 Confidence (round 2)

**UI agent: HIGH.** With 6 specific items round 1 had wrong now
corrected (filter independence in Thetis is real, diversity does
NOT disable RX2 control, etc.), the v0.1 UI is well-mapped. Main
remaining unknowns are visual polish details that surface during
implementation, not architectural surprises.

---

## 7. Phase 4 — RX2 polish (v0.1.0)

**Goal:** Ship v0.1.0 as the new default.

- Splitter geometry persistence per layout
- Per-band-per-RX zoom recall integration (matches Thetis pattern)
- Operator settings for `MuteRX1OnVFOBTX` / `MuteRX2OnVFOATX`
- AAmixer state indicator badge (small; deliberate addition vs Thetis)
- TCI server: surface RX2 frequency on its own channel
- Documentation pass — `docs/help/rx2.md` (new), update `getting-
  started.md` and `index.md`
- Final flight test: 30+ minute sessions with various RX1/RX2
  band/mode combinations; CW + SSB simultaneously; SPLIT
  operation on a contest weekend if available
- CHANGELOG entry, version bump, installer build, GitHub release

### 7.1 Meter system cleanup (RX-side)

The Lit-Arc multi-meter system (`lyra/ui/smeter.py`) currently
ships three styles (Lit-Arc, LED bar, Analog needle) with three
RX modes wired live (S, dBm, AGC).  Polish work lands in v0.1.0:

**(a) Remove the Analog needle meter.** Operator feedback: the
analog face is "kind of horrid looking" and harder to calibrate
than the LED bar or Lit-Arc.  The help doc (`docs/help/smeter.md`)
already labels it "slated for removal."  v0.1.0 strips it.

**(b) Wire AGC signal to all remaining styles.** Today
`radio.agc_action_db` connects to `litarc_meter.set_agc_db` only
(`panels.py:3598`).  The LED bar's `set_agc_db` exists but isn't
connected.  Five-line fix in `panels.py` brings the LED meter to
parity.

**(c) Add Compression mode to Lit-Arc.** New `MODE_COMP` chip,
fed by `radio._leveler._env_db` exposed via a new
`Radio.compression_db` signal.  RX-side, no TX dependency.
Useful when the audio leveler is doing meaningful work on
QRN-heavy bands.  Color palette: cool / neutral (similar to
AGC's blue gradient, distinct hue so the two are
operator-distinct).

**(d) Lit-Arc `AVAILABLE_MODES` after polish:** `(S, dBm, AGC,
Compression)`.  TX modes (PWR, SWR, MIC, ALC) join the list when
v0.2 ships — see §8.4.

### 7.2 Documented Lyra-vs-Thetis meter divergence

Thetis meter modes (verified at `enums.cs:163-191`,
`console.cs:5635-5662`):

- `MeterRXMode`: Signal / Sig Avg / ADC L / ADC R / ADC2 L /
  ADC2 R / Off  (default = Signal)
- `MeterTXMode`: Fwd Pwr / Mic / EQ / Leveler / Lev Gain / CFC /
  CFC Comp / COMP / ALC / ALC Comp / ALC Group / Off
  (default = Fwd Pwr; 12 modes total)
- `MultiMeterDisplayMode`: Original / Edge / Analog (single-
  meter-at-a-time in all three styles — Thetis never shipped a
  true multi-meter view)

Lyra's deliberate divergence: **curated subset + simultaneous
multi-meter view in TX**.  Operators don't read 12-item TX
dropdowns; they want the four to five measurements that matter
all visible at once.  Lyra ships a 5-row stacked TX view (§8.4)
where Thetis ships an 11-meter dropdown.

---

## 8. v0.2 TX path

**Goal:** TX (SSB → CW → AM → FM → leveler/EQ) over multiple
sub-releases. Detail per CLAUDE.md §7.

### 8.1 RX2 touchpoints in v0.2

- RX2 audio routing rules already in AAmixer (phase 2). When TX
  starts:
  - If `MuteRX1OnVFOBTX = on` and TX VFO is VFO B → mute RX1 audio.
  - If `MuteRX2OnVFOATX = on` and TX VFO is VFO A → mute RX2 audio.
  - Otherwise both keep playing during TX (operator can monitor).
- Channel 4 (TX/TXA) activates. New module: `lyra/dsp/tx_channel.py`
  (path mirrors Thetis's TXA structure: phrot → eqp → leveler →
  cfcomp → compressor → bandpass → iqc → resampler).
- TX freq derivation uses the pure function from §6.2.

### 8.2 TX I/Q tap (mandatory for v0.3)

Round-2 DSP agent flagged this as a "future-proofing" requirement
that's cheap to add now but expensive to retrofit. Phase 8.2:

- Add a tap point in `tx_channel.py` between the `bandpass` block
  and the (placeholder) `iqc` block.
- Tap copies post-bandpass / pre-iqc TX I/Q samples into a ring
  buffer.
- In v0.2, the ring buffer is allocated and filled but never read.
- In v0.3, calcc reads from this ring buffer to align with feedback
  samples.

Why this must be in v0.2: refactoring the TX path in v0.3 to add a
tap means re-validating every TX sub-mode (SSB/CW/AM/FM) for
behavior change. Adding the tap as a no-op in v0.2 means v0.3 can
focus purely on PS work.

### 8.3 v0.2 protocol items

Items round 2 protocol agent identified that are HL2-specific and
NOT in earlier Lyra docs:

- **Frame 17** (`tx_latency` + `ptt_hang`, addr 0x17,
  networkproto1.c:1162-1168). C3 = `ptt_hang & 0x1F` (5-bit, 0-31).
  C4 = `tx_latency & 0x7F` (7-bit, 0-127, ms units). Per-rig
  set-and-forget; expose in Settings → TX. Defaults TBD (verify
  against Spark/Quisk; Thetis source doesn't show defaults).
- **Frame 18** (`reset_on_disconnect`, addr 0x3a,
  networkproto1.c:1170-1176). HL2-only graceful shutdown register.
- **CWX PTT bit 3 in I-LSB** (networkproto1.c:1247-1259). On HL2,
  TX I-sample LSB carries CW state bits during CW: bit 3 = cwx_ptt,
  bit 2 = dot, bit 1 = dash, bit 0 = cwx. Non-HL2 has only 3 bits.
- **EER mode overwrites LR audio buffer** during TX
  (networkproto1.c:1224-1226). EER + TX clobbers the audio path.
- **Duplex always on**: `C4 |= 0x04` regardless of XmitBit. EP2
  always streams full-rate; XmitBit (C0 bit 0) controls PA
  keying, not audio framing.

### 8.4 TX multi-meter activation

When v0.2 SSB ships, the meter system gets its TX-side data feed
populated.  Three things land together:

**(a) Forward power + SWR signals.**  HL2 already sends raw
`fwd_pwr_adc` and `rev_pwr_adc` in EP6 telemetry frame 0x10.
Lyra's `FrameStats.fwd_pwr_adc` / `.rev_pwr_adc` parse these as
raw ADC counts (live since v0.0.9.x).  v0.2 adds:

- **Operator-tunable calibration constants** for `fwd_w =
  cal_a_fwd × fwd_pwr_adc² + cal_b_fwd × fwd_pwr_adc + cal_c_fwd`
  (quadratic fit; cal vary per HL2 board revision per CLAUDE.md
  §3.7).  Self-cal workflow lives in Settings → TX → Calibrate.
- **`Radio.tx_pwr_w_changed(float)` signal**: emits computed
  watts when telemetry updates.
- **`Radio.tx_swr_changed(float)` signal**: emits SWR computed
  from forward + reverse:
  `rho = sqrt(rev_w / max(fwd_w, eps)); swr = (1 + rho) / max(1 - rho, eps)`.
- **Wiring** (`panels.py`): connect both signals to LitArc and
  LED meter setters, same pattern as `smeter_level` /
  `agc_action_db`.

**(b) Mic level + ALC + Compression signals.**  TX path
provides:

- `Radio.tx_mic_db_changed(float)`: pre-leveler mic input peak
  in dB.  Source: TX channel input gain stage.
- `Radio.tx_alc_db_changed(float)`: ALC headroom remaining
  (positive = room to drive louder, negative = clipping).
  Source: TX channel ALC block (`SetTXAALCSt` equivalent).
- `Radio.tx_comp_db_changed(float)`: speech compressor gain
  reduction, dB.  Lands with the WDSP `compress.c` port in
  v0.2.1 (CLAUDE.md §4.1 already targeted this).

**(c) Auto-style-switch on MOX.**  When MOX engages, meter
auto-switches to **LED bar style** (5 stacked rows visible at
once).  When MOX releases, auto-switches back to operator's
RX-mode preference (default Lit-Arc).  This matches modern
rig UX (FT-991, IC-7300, ANAN G2) and gives operators
glance-readable TX-side telemetry without chip-clicking.

**Operator override:** Settings → Visuals → Meter behavior:
- "Auto-switch to LED on TX" (default ON)
- "Stay in current style during TX" (operator picks style for
  both RX and TX)

**LED bar TX layout** (rows top → bottom; matches Lyra's
existing `LedBarMeter` scaffolding):

| Row | Live data source | Scale |
|---|---|---|
| **PWR** | `tx_pwr_w_changed` | 0..200 W |
| **SWR** | `tx_swr_changed` | 1.0:1 .. ∞ (non-linear, red past 2:1) |
| **MIC** | `tx_mic_db_changed` | -60..+5 dB |
| **COMP** | `tx_comp_db_changed` | 0..30 dB compression |
| **ALC** | `tx_alc_db_changed` | -10..+3 dB headroom |

The S row is dimmed (no audible RX during TX in normal config).
AGC row is also dimmed (RX-only metric).

**Lit-Arc TX mode chips** (for operators who prefer the big
single-arc readout): `AVAILABLE_MODES_TX = (PWR, SWR, MIC,
COMP, ALC)`.  Default chip on TX entry: PWR.

### 8.5 Confidence (round 2)

**Protocol agent: 80%.** Mic decimation persistence and I2C inline
branch are easy to miss. Frame 17 / 18 HL2-only registers are not
in older HPSDR docs.

**DSP agent: MEDIUM-HIGH.** iqc state machine clear (verified
RUN=0, BEGIN, SWAP, END, DONE order in `iqc.c:113-120` —
**corrected from round 1's BEGIN/SWAP/RUN/END/DONE**). Risk in
TX-pre-iqc tap placement and iqc coefficient swap atomicity.

---

## 9. v0.3 PureSignal

**Goal:** Pre-distortion calibration + correction for HL2 with the
PA-tap hardware mod installed.

### 9.1 v0.3 architecture summary

- **calcc.c port** (`lyra/dsp/ps_calcc.py`, ~1164 LOC C → ~600 LOC
  Python with NumPy). Consumes time-aligned TX-pre-iqc and feedback-
  post-DDC snapshot buffers; produces predistortion coefficient set.
- **iqc.c port** (`lyra/dsp/ps_iqc.py`, ~315 LOC C). Applies
  coefficient set to TX I/Q stream. 5-state lifecycle: RUN, BEGIN,
  SWAP, END, DONE.
- **xbuilder port** (cubic-spline coefficient builder from
  `lmath.c`).
- **delay.c port** (delay line for sample-time alignment).

### 9.2 v0.3 operator flow

- Settings → PS reveals the PS dialog (operator self-attestation
  checkbox per CLAUDE.md §6.5).
- 8-state command FSM (PSForm.cs:111-121): OFF, TurnOnAutoCalibrate,
  AutoCalibrate, TurnOnSingleCalibrate, SingleCalibrate, StayON,
  TurnOFF, IntiateRestoredCorrection.
- 3-state auto-attenuator FSM (PSForm.cs:122-127): Monitor,
  SetNewValues, RestoreOperation.
- Run on `QTimer` at 10 ms (NOT a `Thread.Sleep(10)` busy loop with
  `AboveNormal` priority — Thetis's pattern is showing its age;
  Lyra integrates with Qt event loop instead per §11).
- `lblPSInfoFB`-equivalent visual: feedback level color pulse fading
  5 RGB units per tick when no calibration in progress.
- Per-band/per-rig correction snapshot Save/Restore via QSettings.

### 9.3 Mandatory bench-test gates

**Round 2 protocol agent confidence for v0.3 is 60%** — the
following three items MUST be tested on N8SDR's HL2+ hardware
before declaring v0.3 ready. Without these tests, first-try
execution is not realistic:

#### Gate 1: PS hardware-mod feedback source switch

The `cntrl1` bit that switches HL2's feedback ADC from antenna
sense to TX coupler sense is **NOT a direct register**. It propagates
via:

- Console.cs:8486 sets `rx_adc_ctrl1 |= 0x04` (HL2 nddc=4 path).
  (5-DDC ANAN uses 0x08 instead; not relevant for HL2.)
- This combined value (`P1_adc_cntrl`) is split across **two C&C
  bytes** in frame 4 (addr 0x0e):
  - **C1 = `P1_adc_cntrl & 0xFF`** — lower 8 bits (`cntrl1`)
  - **C2 = `(P1_adc_cntrl >> 8) & 0x3F`** — upper 6 bits (`cntrl2`)
    masked to 0x3F (NOT 0xFF — the upper 2 bits of C2 are
    reserved/used by other fields)
- Both bytes must be written every frame-4 emission. Writing only
  C1 misses `cntrl2`, which on HL2 carries TX-step-attenuator and
  related bits. Source: networkproto1.c:1015-1020.

**Bench test:** With PS armed but no signal, sweep TX from low to
high while monitoring DDC2/DDC3 sample magnitudes via a
diagnostic. **Pass criteria:**
- Magnitudes track TX power level (>20 dB swing across a 30 dB
  TX power sweep) → `cntrl1` routing worked.
- Magnitudes flat (within 6 dB across the same sweep) →
  `cntrl1` did not propagate; check both C1 and C2 byte writes
  in frame 4.

#### Gate 2: PS rate disconnect

Two rates exist:

- **Gateware DDC rate**: `rx[0].sampling_rate` for HL2 (per Lyra
  CLAUDE.md §3.8; verified by round-2 protocol agent).
- **calcc input rate hint**: 192000 (constant from
  `cmaster.cs:316/424-437`).

These are NOT the same number on HL2 unless RX1 happens to be at
192k. Lyra's `ps_calcc.py` MUST be told the actual gateware rate,
not 192000. **Or** the host must resample feedback to 192k before
calcc.

**Bench test:** Run PS calibrate at RX1 = 96k. **Pass criteria:**
- Coefficient magnitudes finite (no NaN, no Inf).
- Coefficient magnitudes within 0.1× to 10× the expected gain
  envelope for the operator's TX power level — anything outside
  this range indicates calcc consumed garbage feedback.
- Post-correction residual at the spectrum analyzer:
  3rd-order IMD products at least **−30 dBc** below the two-tone
  carriers (typical untreated HL2 sits at −20 to −25 dBc; PS
  should improve this by 10+ dB).
- Auto-attenuator FSM completes one full Monitor → SetNewValues
  → RestoreOperation cycle within 5 seconds.

If any of these fail at RX1=96k but pass at RX1=192k, the
rate-hint is wrong; calcc is being told 192000 but the gateware
delivered 96000. Fix by passing actual rx1_rate to calcc.

#### Gate 3: `twist` interleave correctness

The DDC2+DDC3 → audio-chain-1 dispatch uses `twist(spr, 2, 3, 1)`
(networkproto1.c:383-387). The interleave produces a 4-channel
buffer `(I_DDC2, Q_DDC2, I_DDC3, Q_DDC3)`. calcc consumes these
as a complex pair: `feedback[i] = I_DDC2[i] + j×Q_DDC2[i]` for
the first feedback channel, `I_DDC3[i] + j×Q_DDC3[i]` for the
second.

If Lyra's `twist` implementation pairs `(I_DDC2, Q_DDC3)` instead,
calcc's correlation math sees garbage feedback and diverges.

**Bench test:** With a known CW carrier on the dummy load, verify
DDC2 I/Q pair shows the carrier at expected baseband freq, and
DDC3 I/Q pair likewise. Cross-pairing would show two phase-
inconsistent signals.

### 9.4 Confidence (round 2)

**Protocol agent: 60% without bench tests, ~85% with.**
**DSP agent: MEDIUM** (calcc snapshot geometry + time-alignment).
**UI agent: adequate** (PSForm pattern is well-mapped).

If gates 1, 2, 3 pass on bench testing before v0.3 development
begins, confidence on first-try success rises to ~85% across all
three layers.

---

## 10. Errors corrected (round 1 → round 2)

| Round 1 claim | Round 2 correction | Source |
|---|---|---|
| TX channel ID = 10 (CMsubrcvr × CMrcvr = 2 × 5) | TX channel ID = 4 (2 × 2 normal config) | dsp.cs:926-945 |
| iqc state order: BEGIN/SWAP/RUN/END/DONE | RUN=0, BEGIN, SWAP, END, DONE | iqc.c:113-120 |
| Pan slider was a contradiction | TWO different pans: `SpecRX.PanSlider` (display, global) vs `Receiver.Pan` / `SetRXAPanelPan` (audio, per-RX). Both correct in their layer. | console.cs:34301-34313 vs patchpanel.c:158-176 |
| 3 priming C&C frames at startup | 6 priming frames (3 TX-VFO + 3 RX-VFO in 2 batches with 10ms gaps) | networkproto1.c:134-139, 430 |
| TX freq carried in frames 1, 5, 6 | Frames 1, 5, 6, AND 7 (DDC4) on HL2 | networkproto1.c:974, 1029, 1039, 1049 |
| PS feedback `cntrl1` is a direct register | Propagates via `P1_adc_cntrl` byte in frame 4 (addr 0x0e) — silent failure mode if missed | networkproto1.c:1015-1020, console.cs:8486 |
| PSrate=192000 vs rx1_rate apparent conflict | Both true: gateware uses rx1_rate, calcc-input is told 192000 — host-layer rate disconnect that must be resolved | cmaster.cs:316/424-437 vs CLAUDE.md §3.8 |
| Diversity removes RX2 from operator UI | Diversity reconfigures routing but `chkRX2` stays operator-toggleable | DiversityForm.cs:2576-2590, console.cs:10775-10805 |
| RX2 inherits filter from RX1 | RX2 has full independent filter selector (128 `radRX2Filter*` controls in Thetis). Lyra's "single Mode+Filter on focused RX" is a deliberate **divergence** | console.cs (filter handlers) |

---

## 11. Patterns vs divergences

### 11.1 Adopt verbatim from Thetis

- Channel ID convention (with corrected TX = 4)
- TXFreq derivation pure function (§6.2)
- AAmixer state matrix concept (with Lyra's own indexing scheme)
- Per-channel patchpanel pan curve (`gain1=1, gain2=sin(pan·π)` /
  symmetric)
- iqc 5-state lifecycle (RUN, BEGIN, SWAP, END, DONE)
- PS 8-state command FSM + 3-state AA FSM
- `MuteRX1OnVFOBTX` / `MuteRX2OnVFOATX` as independent operator
  toggles
- Force `VFOATX = true` when SPLIT + RX2 + VFOBTX
- Per-band-per-RX zoom recall pattern
- TUN button label flip ("TUN" ↔ "AUTO")

### 11.2 Deliberate divergences

- Hybrid focus model (single Mode+Filter / DSP+Audio panel + per-
  RX badges + Ctrl+1/Ctrl+2) instead of duplicated controls
- Per-RX display pan instead of global
- Visible AAmixer state badge instead of operator-infers-it
- PS hardware-mod attestation checkbox
- QSplitter for panadapter regions instead of skin percentages
- PS FSM on `QTimer` 10ms instead of `Thread.Sleep(10)` AboveNormal

### 11.3 Do NOT copy

- WinForms Designer .resx-driven layout (no Qt equivalent needed)
- Sub-VFO repurposing the VFO A band label
- Color-only state indicators without text/icon labels (accessibility)
- 8-state FSM with shared mutable globals
- Duplicated control widgets per RX

---

## 12. Cross-references

- `CLAUDE.md` §3 — HL2 protocol critical facts (this doc adds
  corrections per §10 and additional HL2 quirks per §4.3, §8.3).
- `CLAUDE.md` §6 — Architectural decisions (this doc supersedes
  §6's RX2 plan with the consensus phases).
- `CLAUDE.md` §7 — Phased delivery roadmap (this doc fleshes out
  v0.1, v0.2 RX2 touchpoints, v0.3 PS bench-test gates).
- `CLAUDE.md` §10 — Open empirical questions (this doc adds the
  three v0.3 bench-test gates as REQUIRED before declaring v0.3
  ready).
- `docs/architecture/implementation_playbook.md` — original RX2/
  TX/PS spec (this doc is the consensus update; reference both).
- `docs/architecture/v0.0.9_rx2_plan.md` — superseded by this doc.
- `docs/architecture/rx2_research_notes.md` — historical first-pass
  research; this doc is the authoritative replacement.
- `docs/architecture/hl2_puresignal_audio_research.md` — HL2 PS
  audio chain research; relevant for v0.3.
- `docs/architecture/wdsp_integration.md` — attribution patterns
  for ports from WDSP source (calcc, iqc, xbuilder, delay).

---

**Last edited:** 2026-05-05 by Lyra-SDR three-engineer review.

Future updates: when phases land, append a "phase complete" subsection
under that phase with: actual implementation files, deviations from
this plan, surprises encountered. Don't rewrite the plan — accumulate
field reports under it. The plan stays authoritative until explicitly
superseded.
